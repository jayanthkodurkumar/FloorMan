version: "3.9"

services:
  backend:
    build: ./server
    env_file: ./server/.env
    ports:
      - "8000:8000"

  frontend:
    build: ./client
    ports:
      - "5173:5173"
    depends_on:
      - backend
